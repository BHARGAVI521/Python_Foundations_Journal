def fibo_series():
    if a==1:
        return [0]
    elif a<0:
        return[]
    else:
        fibo=[0,1]
        for i in range(0,a-2):
            new=fibo[-1]+fibo[-2]
            fibo.append(new)
        return fibo
a=int(input("Enter number of digits : "))
print(fibo_series())
